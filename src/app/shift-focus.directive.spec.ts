import { ShiftFocusDirective } from './shift-focus.directive';

describe('ShiftFocusDirective', () => {
  it('should create an instance', () => {
    // const directive = new ShiftFocusDirective();
    // expect(directive).toBeTruthy();
  });
});
